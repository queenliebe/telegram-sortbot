services:
  - type: web
    name: telegram-sortbot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python telegram_sortbot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9